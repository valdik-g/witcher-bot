# frozen_string_literal: true

# module for getting list of prerecordings
module GetPrerecording
  def get_prerecording(chat_id, bot)
    # prerec = Prerecording.last
    # message = prerec.closed ? 'Предзапись уже закрыта' : close_message(prerec)
    # bot.api.send_message(chat_id: chat_id, text: message)
  end
end
